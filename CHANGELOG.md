# 版本时间线

项目目前仍在自己使用和持续完善的阶段，所以正式版本号都从 `0.x.x` 开始：

- 增加一批重要功能：中间一位加 1，例如 `0.1.0 → 0.2.0`。
- 修正问题或做一批小优化：最后一位加 1，例如 `0.2.0 → 0.2.1`。
- 真正正式发布、交给市场检验时，再进入 `1.0.0`。

## V0.7.0：三和弦反馈解释升级与本地存储修复（2026-08-14）

- 三和弦（Closed）反馈解释升级为完整句式：依次给出根、三、五音音名、转位、从低到高的音名答案、唱名序列与音程关系，例如「根、三、五音是 C-E-G；本题为第一转位，音名为 E-G-C，从低到高是 Mi-Sol-Do，音程关系是 M3-5-R」；大小减三种性质分别使用 Do-Mi-Sol / Do-Me-Sol / Do-Me-Se 与 M3 / m3 / ♭5 标记。
- 新增 triadMemberSequence 底层函数，并重构 triadSolfege 共用同一套按转位旋转三元组的逻辑，消除两处重复拼接。
- 修复本地存储写函数自我递归导致所有数据静默丢失的问题：safeSetItem 误调自身造成栈溢出并被捕获吞掉，设置、错题、进度与统计均不再落盘；改回写入 localStorage，并增加真实写入断言与存储不可用静默跳过两条回归测试。

## V0.6.3：重变化音、三和弦题头与反馈焦点修复（2026-08-12）

- 将单升降、重升重降和三重升降统一建模为一个完整 accidental token，并按 SMuFL 标准分别使用专用字形；修复音程答后试听中多个普通升降号重叠、视觉上退化成单个符号的问题。
- 答后选项试听、反馈解释、Voicing 答案与音级试听统一使用答案语境的音名排版变量，升降号按音名字号约 92% 显示，避免正文小字号下单升降与重升降显得过小。
- 增加 `E♭–B𝄫`、`F♭–E𝄫`、`F♭–E♭♭♭`、`C♯–F𝄪` 等固定低音试听拼写回归，确保显示、理论拼写与实际 MIDI 同步。
- Closed、Spread 与三音·五音题头统一复用七和弦已有的 `ChordSymbol`，移除重复的“D小三和弦（D−）”标题结构。
- 反馈出现后将焦点落到中性反馈区域，而不是“再听一次”或“下一题”按钮；空格固定重播，Enter 固定进入下一题，鼠标点击反馈按钮也不会留下按钮焦点。

## V0.6.2：音程字形、答题焦点与播放时序修复（2026-08-12）

- 修复音程答题后试听其他选项时，升降号被预览区域字体覆盖并显示成方框的问题。
- 调整大字号题目音名的升降号间距，避免 `F♭` 等音名贴得过紧。
- 测验中用鼠标点击播放、模式、音色等辅助按钮时不再夺走答题位置；切换音色后按空格仍会重播，不会再次触发音色按钮。
- 延迟音符改用 Tone 音频上下文时钟调度；旋律与琶音增加 80ms 稳定起播缓冲，减少初次解码或页面渲染造成的开头抢拍，同时继续支持完整取消。

## V0.6.1：乐理排版、音高修复与音源规则解耦（2026-08-12）

- 将练习播放设置、乐器演奏行为与采样包资产拆为三层；用户仍只选择稳定的“钢琴 / 古典吉他”，以后更换采样包不需要迁移练习规则。
- 移除实测高约一个半音的古典吉他 D5 错误锚点，D5 改由 Tone.js 从正确锚点插值；补充 D 大三和弦与双音源目标 MIDI 一致性测试。
- 和弦标记改为完整的文字与符号两套体系，覆盖 `dim / °`、`m7♭5 / ø7`；普通音名、和弦根音、键盘升降号和成员序列使用统一语义组件排版。
- 旋律/和声及琶音/和声切换会停止旧声音并立即以新模式重播；测验中统一使用 Enter 提交或进入下一题、空格重播。
- 科学音高显示从全局顶栏迁入音程判断；音级反向题答题后可点击 1–7 级试听对应单音。
- 设置存储升级并自动迁移旧的科学音高与和弦标记设置；错题、统计、乐器和原有播放偏好保持不变。

## V0.6.0：全局音色、统一键盘与音乐符号（2026-08-12）

- 音色切换移到顶栏，点击当前音色即可在钢琴与古典吉他之间切换；切换会停止旧声音，并在测验中用新音色重播当前题。
- 钢琴改用 Salamander Grand Piano V3 原始 48 kHz / 24-bit 第 10 力度层的八个锚点；古典吉他改用 Tone.js Instruments 整理的 Quartertone Yamaha Classical Guitar 十三个锚点。
- 为两套音源分别配置响度、三段均衡与释放时间；钢琴和声同时触键，吉他继续以约 30ms 从低到高轻扫。
- 音名、和弦成员、音级、和弦进行质量与音程选项均可用电脑键盘操作；填空完整后可在答案格内按 Enter 提交，外部播放和设置控件保留原生键盘行为。
- 非五线谱区域的升降号与爵士和弦符号改用 Bravura / SMuFL 字形；根音、升降号和和弦性质由语义组件分别排版，五线谱渲染保持不变。
- V–I–目标音播放单独放慢到原速度的 85%；四小节和弦进行仍保持约 123 BPM。
- 和弦进行反馈中的正确答案改为 24–28px 高对比度答案块。
- 进阶音程移除增二度、增减三度、减四度、增减六度和增七度；保留增四、减五、增五与减七等实用变化音程。
- `App` 只保留会话、页面、统计、错题与播放编排；音程、三和弦和七和弦练习视图、音色注册表、音乐文字与答题热键协议均拆为独立模块。
- 新音源、Bravura 字体和完整许可说明进入 PWA 离线缓存；旧设置、统计、错题及题目身份保持兼容。

## V0.5.0：标准五线谱与记谱核心重构（2026-08-11）

- 调内音级与和弦进行谱面按照五度圈显示正确调号；独立音程与和弦保持无调号并使用临时升降号。
- 临时升降号按标准小节规则处理：不重复调号内升降号，需要时显示还原号，并在小节线后重置。
- V–I–目标音改为无拍号的音高示意，不再用三个四分音符虚构节拍。
- 四小节和弦进行改为真实 `4/4` 记谱，每小节一个全音符和弦，并加入小节线与终止线。
- 和弦进行在窄屏下按两行、每行两小节排版，换行后重复谱号与调号。
- 增加独立于 React、VexFlow 与音频服务的谱面数据模型和题目适配层，为未来移动端渲染保留边界。
- 穷举验证十二大调调号、调内拼写、临时升降号状态及谱面与播放音高一致性。
- 用户可见调名统一使用标准 `F♯ / B♭` 符号，内部调名 ID 保持兼容旧设置。

## V0.4.1：测验返回、Triad 播放与键盘操作优化（2026-08-11）

- “× 结束”会按当前模块返回上一层练习选择；错题复习结束后返回错题复习页，顶部 Logo 与总结页仍返回首页。
- Closed 与 Spread Triad 共用持久化的“旋律 / 和声”播放设置，默认使用和声播放。
- 音名填空支持电脑键盘输入：`A–G`、`b / s / # / n`、`Backspace`，以及四个方向键和 `Tab / Shift+Tab` 循环切换输入框。
- 首页和弦图标改为轻量微型五线谱；Drop 2 图标以第二高音下移的谱面动作取代 `D2` 文本。
- 三和弦图标按高音谱表 `E–G–B` 叠三度绘制；Drop 2 图标按 `G–B–D–F` 将第二高音 D 精确下移到低八度 D。
- 调内音名与音级题显示 V–I 终止及目标单音谱面；和弦进行题显示实际播放的四组 Voicing 谱面。
- 音程及七和弦设置页移除重复的“默认播放”，播放方式只在答题页切换。
- 退出测验时会停止声音、取消延迟音符并清理当前会话状态。

## V0.4.0：和弦层级、Spread Triad 与双音源（2026-08-11）

- 和弦模块改为“三和弦 / 七和弦”逐级导航；Closed Triad、Spread Triad、三音·五音成为三个独立测验。
- 增加 Spread Triad 音名填空，覆盖 `R–5–3、3–R–5、5–3–R`，并支持大、小、减三和弦。
- Closed 与 Spread 每轮十题都覆盖全部三种排列，并在所选和弦性质间尽量均衡；两者的错题和去重身份相互独立。
- 三音·五音测验固定播放 Closed 原位，不再混在其他和弦测验的设置中。
- 增加 FreePats CC0 古典吉他音源，可在所有练习的播放按钮旁与钢琴切换；选择会跨会话保存。
- 钢琴和声同时触键，古典吉他和弦与和弦进行以约 30ms 从低到高轻扫；切换或停止会取消尚未发出的延迟音。
- 进阶音程不再生成或显示“增七度”，其他增、减音程保留。
- 增加“音源与许可”说明，钢琴继续使用 CC BY 3.0 的 Salamander Grand Piano V3，吉他使用 CC0 1.0 的 FreePats 采样。
- 将和弦层级与设置页拆为独立组件，并加入 Ionian 调式定义和进行分类目录，为后续调式与借用和弦扩展保留数据接口。

## V0.3.0：增加七和弦与爵士 Voicing（2026-08-11）

- 增加 Drop 2 七和弦听辨，覆盖 `5R37、37R5、735R、R573` 四种排列。
- 增加 Shell Chord 听辨，覆盖 `R37、R73` 两种排列。
- 可以练习大七、小七和属七，并判断三音、七音是否需要降号。
- Drop 2 和 Shell Chord 都可以切换琶音或和声播放。
- 和弦进行增加 Shell 和 Drop 2，七和弦模式会自动选择连接更顺的排列。
- 增加一般标记和爵士标记切换，例如 `Cmaj7 / C△`、`Cm7 / C-7`。
- 把和弦练习入口合并到一个设置页，减少多层菜单。
- 保留旧设置、错题和统计，并补齐七和弦、音频与窄屏测试。

## V0.2.1：优化播放、音域、去重和速度（2026-08-08）

- 每次进入下一题或退出练习，自动停止上一题还没有播放完的声音，避免音频叠在一起。
- 把主要播放音域控制在下加一线附近到高八度 F，减少过高、刺耳的声音。
- 前后两题不会出现完全相同的内容，也会避开上一轮最后一题和下一轮第一题重复。
- 四小节和弦进行的播放速度提高到原来的约 1.4 倍，让练习更紧凑。

## V0.2.0：增加调内音级与和弦进行（2026-08-08）

- 在“这个和弦的三音/五音是什么”题目中，把“三音”和“五音”加粗并换成更容易看清的颜色。
- 理顺五度圈范围、调和练习内容之间的关系，增加调内音名与音级的双向问答。
- 可以练习“E 调的 6 级是什么音名”，也可以根据给出的音名填写它在调内的音级。
- 增加符合调内逻辑的和弦进行，可以在音名和级数之间双向作答。
- 修正答题后试听与原题不匹配的问题：固定低音，只改变高音，再播放对应音程。
- 改进题目生成与随机选择，避免固定顺序造成可以猜答案的感觉。

## V0.1.0：建立基础听辨与三和弦练习（2026-08-06）

- 答对后会播放一次正确答案。
- 修正和弦原位与转位的选择机制，让题目真正随机出现。
- 音程题的选项位置保持固定，并根据本轮选择的测试范围缩减可用选项。
- 答对或答错都会给出声音或动画反馈，但反馈保持克制，不会过分突出。
- 增加科学音高标记开关，可以选择显示 `C4`，也可以隐藏数字只显示 `C`。
- 答完音程题后可以点击选项查看音名，并播放对应音程。
- 和弦解释增加唱名表示，例如 `Do-Mi-Sol`、`Mi-Sol-Do`、`Do-Me-Sol`。
- 和弦填音完成后，不论答对或答错，都可以点击已经填写的音来试听，也可以播放自己的答案。
